export enum EGlobalSizes {
  NONE = '0rem',
  SMALLEST = 'var(--s-5)',
  TINY = 'var(--s-4)',
  XXS = 'var(--s-3)',
  XS = 'var(--s-2)',
  SMALL = 'var(--s-1)',
  BASE = 'var(--s0)',
  MEDIUM = 'var(--s1)',
  LARGE = 'var(--s2)',
  XL = 'var(--s3)',
  XXL = 'var(--s4)',
  LARGEST = 'var(--s5)',
  MEASURE = 'var(--measure)'
}

export enum EJustifyMap {
  START = 'flex-start',
  END = 'flex-end',
  CENTER = 'center',
  BETWEEN = 'space-between',
  AROUND = 'space-around',
  EVENLY = 'space-evenly',
  STRETCH = 'stretch'
}

export enum IAlignMap {
  START = 'flex-start',
  END = 'flex-end',
  CENTER = 'center',
  STRETCH = 'stretch',
  BASELINE = 'baseline'
}

export enum EAlignContentMap {
  START = 'flex-start',
  END = 'flex-end',
  CENTER = 'center',
  STRETCH = 'stretch',
  BETWEEN = 'space-between',
  AROUND = 'space-around'
}
